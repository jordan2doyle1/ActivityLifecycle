apply plugin: 'com.android.application'

android {
    signingConfigs {
        release {
            storeFile file('/Users/jordandoyle/AndroidStudioProjects/KeyStore')
            storePassword 'Compscience13!$'
            keyAlias = 'ActivityLifecycleKey'
            keyPassword 'Kildare!((%2018'
        }
    }
    compileSdkVersion 30

    defaultConfig {
        applicationId "com.example.android.lifecycle"
        minSdkVersion 24
        targetSdkVersion 30
        signingConfig signingConfigs.release
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}

dependencies {
//    implementation files('libs/android-support-v13.jar')
    implementation 'com.google.android.gms:play-services-location:18.0.0'
}
